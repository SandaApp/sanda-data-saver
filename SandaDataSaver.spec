# -*- mode: python ; coding: utf-8 -*-


a = Analysis(
    ['sanda_pc_app.py'],
    pathex=[],
    binaries=[],
    datas=[('assets\\sanda_logo.png', 'assets'), ('assets\\sanda_icon.ico', 'assets'), ('VERSION.txt', '.')],
    hiddenimports=[],
    hookspath=[],
    hooksconfig={},
    runtime_hooks=[],
    excludes=[],
    noarchive=False,
    optimize=0,
)
pyz = PYZ(a.pure)

exe = EXE(
    pyz,
    a.scripts,
    a.binaries,
    a.datas,
    [],
    name='SandaDataSaver',
    debug=False,
    bootloader_ignore_signals=False,
    strip=False,
    upx=True,
    upx_exclude=[],
    runtime_tmpdir=None,
    console=False,
    disable_windowed_traceback=False,
    argv_emulation=False,
    target_arch=None,
    codesign_identity=None,
    entitlements_file=None,
    uac_admin=True,
    icon=['assets\\sanda_icon.ico'],
)
